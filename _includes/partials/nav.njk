<div class="container mt-3">
    <nav class="nav justify-content-center">
    {% for item in metadata.navbar.header_list %}
        <a class="nav-link{{ ' border-end' if not loop.last }}" href="{{ item.url }}">
            {{ item.nav }}
        </a>
    {% else %}
        <a class="nav-link" href="{{ metadata.url }}">Home</a>
    {% endfor %}
    </nav>
</div>